# ── 필수 의존성 ──────────────────────────────────────────
sounddevice==0.5.1
soundfile==0.13.1
numpy
google-genai>=1.0.0
google-auth>=2.23.4
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1
google-api-python-client>=2.108.0
Pillow
requests>=2.31.0
anthropic
openai>=1.0.0
markdown>=3.5.0
pyinstaller>=6.0.0

# ── 선택적 의존성 (미설치 시 해당 기능만 비활성화) ─────────
# weasyprint>=60.0       # PDF 직접 변환 (미설치 시 HTML로 자동 대체)
# pywin32>=300           # 네트워크 프린터 고급 제어 (미설치 시 lpr 명령어로 대체)
